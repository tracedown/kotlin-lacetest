rootProject.name = "lacelang-kt-lacetest"

// The validator and executor are resolved as published artifacts
// (dev.lacelang:kotlin-validator / :lacelang-kotlin-executor) from Maven
// Central, or from the local Maven repository (`publishToMavenLocal`) during
// development.
